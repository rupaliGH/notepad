alert popup
 Alert alt=driver.SwichTo.alert();
 alt.accept();
 alt;dismiss();
 alt.gettext();
 
 1 indentify listbox to be handle and store in one reference variable
 2 create object of Select class which accept the webelement as argument
 3 using any method of slect class we can perfom actiion on dropdown
 methods are-
 slectbyvisibletext
 selectbyindex,
 selectbyvalue,
 
 //Screnshot
 File SRC=((TakesScreenshot)driver).getscreenshotAs(Outputtype.File);
 String random=RandomSTring.make(4);
 File Dest = new FIle("\\pathofthefolder\\nameofss"+random+".png");
 FileHandler.copy(SRC,Dest);
 
 1 switch selenium focus from main page to frame
 2 driver.switchTo.frame(String name or ID);   main to frame
 3 driver.switchTo.parentframe();  //frame to parent frame
 4 driver.switchTo.defaultcontent();  //frame to main page
 
 //Actions class
 Actions ac= new Actions
 click();
 doubleclick();
 contextclick();
 movetoelement();
 draganddrop?();
 
 keyboard
 keyup
 keydown
 sendkeys
 
 
 
 implicite wait
 //for complete webpage
 syntax
 driver.manage.timeouts.implicitelywait(Duration.ofseconds(500));
 
 explicite wait
 // for single element
 syntax
 create object of Webdriverwait class
 Webdriverwait ww=new webdriver(driver,Duration.ofseconds(200));
 w.until(expectedconditions.visibilityof(reviws))